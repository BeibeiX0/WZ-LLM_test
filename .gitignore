__pycache__/
*.pyc
*.pyo
*.egg-info/
.env
output/
*.log

# Exclude generated jsonl outputs but keep the test set
*.jsonl
!wz_llm/test_set/*.jsonl

# Keep the repl binary
!wz_llm/utils/lean4Kit/repl_v4.25

# Lake build artifacts (large, regenerated by lake build)
.lake/build/

# Lake packages are fetched by lake, don't commit them
# (exception: if you want to pin exact sources, remove this line)
.lake/packages/
